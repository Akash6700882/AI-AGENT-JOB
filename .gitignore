venv/
__pycache__/
__pychache__/
*.pyc
node_modules/
dist/
build/

# Local runtime data — per-user resumes, application DBs, uploaded files.
# Contains real password hashes and resume PII; never belongs in git.
app/backend/data/
app/backend/temp_uploads/

# Secrets — real API keys / JWT signing key / DB password live here locally.
.env
.env.*
